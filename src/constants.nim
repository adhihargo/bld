let
  ## String in an environment variable's list of values that, when
  ## matched, will be replaced with the variable's original value.
  ENV_PLACEHOLDER_ORI* = "*"

  ## Config file name in JSON format
  CONFIG_JSON_NAME* = "bld.json"
  ## Config file name in YAML format
  CONFIG_YAML_NAME* = "bld.yaml"
